rootProject.name = 'onboarding'

include('authentication-service', 'integration-test', 'onboarding-commons', 'onboarding-service')

rootProject.children.each { project ->
    String dir = "projects/$project.name"
    project.projectDir = new File(settingsDir, dir)
}